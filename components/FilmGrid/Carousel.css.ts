import { style } from "@vanilla-extract/css";

export const carouselWrapper = style({
    overflow: 'hidden',
});

export const carouselContainer = style({
    display: 'flex',
});

export const emblaPrev = style({});

export const emblaNext = style({});